/*
// set: bool, u8, i8, u16, i16, u32, i32, u64, i64, u128, i128, usize, isize, f32, f64, char

obj: empty, x in (bool || i32 || usize || f64 || char)

set: bool, i32, usize, f64, char, obj, set, map, func

map:

any in anyset

bool as i32
bool as usize
i32 as f64

i32 + - * %
usize + - * %
f64 + - * / %

function application
Function calls, array indexing
()
!
as
* / %
+ -
== != < > <= >=
in
&&
||
=



* /


*/